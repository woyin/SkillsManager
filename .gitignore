# SQLite database (local state, not shared)
data/*.db
data/*.db-wal
data/*.db-shm
data/backups/

# Go build artifacts
sm
*.exe
/bin/
cover.out

# OS files
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*~
*.tmp

# sm project-scope installs (machine-generated at runtime)
/.agents/
/.claude/skills/

# Claude Code local config (machine-specific permissions, etc.)
.claude/settings.local.json

# External skill lock files (not managed by this CLI)
skills-lock.json
