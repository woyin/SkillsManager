# internal/home 文件夹索引

## 架构说明
工具层，封装用户 home 目录的解析（尊重 HOME 环境变量）。
无状态、无依赖，是几乎所有包定位全局配置根（~/.sm）的基础。

## 文件清单

### home.go
- **地位**: home 目录解析的唯一来源
- **功能**: Dir() 返回用户 home 绝对路径
- **依赖**: （无内部包依赖）
- **被依赖**: internal/tool、internal/installer、cmd/（广泛使用）

---
⚠️ **自指声明**: 当本文件夹内容变化时，请更新此索引
